#include<bits/stdc++.h>
using namespace std;
int sumarr(int arr[],int n){
	int sum=0;
	for (int i=0;i<n-1;i++){
		sum=sum+arr[i];
		i++;
	}
	return sum;
}
int main(){
	int n;
	cin>>n;
	int arr[n];
	for (int i=0;i<n;i++){
		cin>>arr[i];
	}
	int result;
	result=sumarr(arr,n);
	cout<<result;
}

